# Sessione 2026-09-21 — Deploy frontend, Git/GitHub, migrazione account Azure

## Obiettivo della sessione
Pubblicare il frontend Angular su Azure, versionare il progetto su GitHub e, a metà sessione, migrare tutta l'infrastruttura Azure dall'account precedente all'email personale di Luca. Modalità di lavoro cambiata a metà: Luca vuole **essere guidato passo passo ed eseguire lui** i passaggi su Git/Portale Azure per imparare (Claude spiega il concetto e verifica, non fa tutto al posto suo).

## Cosa è stato implementato/modificato

- **Frontend, ambienti**: `environment.ts` ora contiene i valori di produzione (`production: true`, API su Azure); nuovo `environment.development.ts` con `localhost:7026`; `fileReplacements` nella configurazione `development` di `angular.json`, così `ng serve` usa i valori locali e `ng build` (default production) quelli Azure.
- **Frontend, SPA fallback**: nuovo `public/staticwebapp.config.json` con `navigationFallback` su `/index.html` (evita 404 al refresh su rotte tipo `/employees/123`).
- **`.gitignore` di root** (nuovo): `bin/`, `obj/`, `.vs/`, `*.user`, `publish/`, `*.zip`, `node_modules/`, `dist/`, `.angular/`.
- **`README.md` di root** (nuovo, in inglese, orientato a portfolio): funzionalità, algoritmo di matching, architettura e decisioni chiave, stack, layout repo, avvio locale, deploy, roadmap. Luca ha poi rimosso a mano le righe con i link alla demo live.
- **Git**: repository inizializzato con 4 commit su `main`, pushato su `https://github.com/lucagallina17/teamcraft.git` (`Initial commit`, `Changed api fetch url`, workflow SWA aggiunto da Azure, `Added README.md`). Working tree pulito e allineato a `origin/main`.
- **Infrastruttura Azure ricreata sull'account personale** (Luca, via Portale): Resource Group `teamcraft-rg`, SQL Server + Database `TeamCraftDb` (Basic), App Service API (Linux, .NET 8) in **Sweden Central**, Static Web App (Free). Nuovo URL API `https://teamcraft-api-haawg0cfgzdpdxgn.swedencentral-01.azurewebsites.net`, nuova SWA `https://red-stone-04625aa0f.1.azurestaticapps.net`.
- `CLAUDE.md` aggiornato (riga Deploy). Memoria auto di Claude: salvata la preferenza "guided hands-on learning".

## Decisioni architetturali prese e perché

1. **Frontend su Azure Static Web Apps (Free) invece che nello stesso App Service dell'API**: hosting pensato per SPA, HTTPS e CDN gratuiti, fallback SPA integrato, FE e BE disaccoppiati (al costo di dover configurare CORS).
2. **Deploy del frontend con GitHub Actions** (non SWA CLI da locale): CI/CD automatico a ogni push su `main`, valore aggiunto per il portfolio. **Deployment authorization policy = GitHub**, così non c'è nessun deployment token da gestire a mano.
3. **`environment.ts` = produzione, `environment.development.ts` = locale** (convenzione Angular recente): la build di default punta a Azure, lo sviluppo locale resta invariato.
4. **CORS controllato da variabile d'ambiente** `Cors__AllowedOrigins__0` sull'App Service (già leggibile dal codice dalla sessione precedente): l'origin di produzione si aggiunge dal Portale senza ricompilare.
5. **Nessun dato da migrare tra i due account**: il DB era vuoto e l'app applica le migration all'avvio (`Database.Migrate()`), quindi la migrazione è stata "ricrea le risorse", non "sposta i dati".
6. **Non committare né pushare in autonomia**: comandi Git eseguiti da Luca; Claude ha fatto solo dry-run (`git add -n`) e scansione di possibili segreti prima del primo commit (nessun segreto trovato; `.claude/`, `CLAUDE.md`, `log/` inclusi nel repo per scelta di Luca).

## Problemi risolti

1. **`RequestDisallowedByAzure` — "region not accepting new customers"** su West Europe, sia per la Web App sia per la Static Web App: il nuovo account non può creare risorse lì. Risolto scegliendo un'altra regione (Sweden Central per App Service/SQL; per la SWA una regione non europea, dato che le SWA supportano poche regioni e per file statici serviti da CDN la scelta non incide sulla latenza).
2. **`ExternalAdministratorLoginSameAsSqlAdmin`** creando il SQL Server: il login admin SQL non può coincidere con l'admin Microsoft Entra. Risolto scegliendo un nome diverso (es. `teamcraftadmin`).
3. **Box grigi/non modificabili** su Connection strings: la casella nella pagina SQL Database è di sola lettura, si copia e si modifica altrove (poi va incollata come app setting `ConnectionStrings__DefaultConnection` sull'App Service).
4. **CORS apparentemente ancora attivo dopo la configurazione**: verificato con `curl` che l'API rispondeva correttamente (`Access-Control-Allow-Origin` uguale all'origin della SWA, GET 200 su tutti gli endpoint principali, preflight 204) e che il bundle della SWA puntava al nuovo URL API. Il problema era una cache del browser; risolto con ricarica forzata/finestra in incognito.

## Problemi/punti ancora aperti

1. **Risorse del vecchio account Azure ancora da cancellare** (Resource Group, SQL, App Service): se in Free Trial consumano credito inutilmente.
2. **Il profilo di publish di Visual Studio** del vecchio account può essere ancora presente in `Properties/PublishProfiles` del progetto API e potrebbe contenere riferimenti (ID risorse/subscription) al vecchio account; verificare se è stato committato.
3. **Pipeline CI/CD solo per il frontend**: il backend si pubblica ancora a mano da Visual Studio → Publish.
4. **Autenticazione JWT** ancora non implementata.
5. **Budget bundle Angular**: la build di produzione segnala un warning sul budget iniziale (700 kB contro soglia 500 kB, limite d'errore 1 MB) — non bloccante ma da tenere d'occhio.
6. **`.NET 8` LTS termina il supporto a novembre 2026**: pianificare l'upgrade (probabilmente .NET 10).
7. **`src/ai_output_audit.md`** dice ancora "Stack: .NET 7" (documento storico, non aggiornato) e nel repo esiste ancora la cartella residua `src/backend/TeamCraft/TeamCraft.API/` (vecchio scaffold non referenziato dalla `.sln`).
8. **README**: la frase sulla porta locale per `apiUrl` è generica (ci sono 3 profili di avvio in `launchSettings.json`); da precisare quando si sa quale profilo si usa.

---

# Parte 2 — Repo pubblico: pulizia della storia Git e igiene dei dati

Aggiornamento della stessa giornata, dopo la prima chiusura sessione. Il punto 2 dei problemi aperti qui sopra (profili di publish del vecchio account) è **risolto** da questa parte.

## Cosa è stato fatto

- **Scansione dei file tracciati** (`git grep`) prima di rendere pubblico il repo: nessuna email personale né password/token. Trovati invece due profili di publish di Visual Studio (`Properties/PublishProfiles/*.pubxml`) e due `ServiceDependencies/*/profile.arm.json` con gli **ID di entrambe le subscription Azure** (vecchia e nuova).
- **`.gitignore`**: aggiunte `**/Properties/PublishProfiles/` e `**/Properties/ServiceDependencies/`.
- **Storia di `main` riscritta** con `git filter-branch --index-filter 'git rm -r --cached --ignore-unmatch …'` su entrambi i percorsi: stessi 5 commit e stessi messaggi, hash nuovi. Prima della riscrittura è stato creato un branch di backup locale (poi cancellato da Luca a verifica completata, insieme a `refs/original` e al reflog).
- **Push forzato eseguito da Luca** (`git push --force-with-lease origin main`), non da Claude: regola di prudenza sul force-push a `main`.
- **README**: aggiunta in Roadmap la nota sul GUID tutto a zero usato come `CreatedBy` segnaposto finché non c'è la JWT (`project-detail.ts`).
- **Verifica post-push**: `main` locale = `origin/main` (`cfc0d6f`); nessun `.pubxml`/`profile.arm.json` né ID subscription in nessuna delle 5 revisioni del remote; workflow GitHub Actions concluso con `success`; SWA e `/api/employees` rispondono 200.

## Decisioni prese e perché

1. **Repo pubblico, e si tengono pubblici `CLAUDE.md`, l'agente `.claude/agents`, la memoria, `log/` e `src/ai_output_audit.md`**: per il CV mostrano un metodo di lavoro con l'AI (istruzioni, verifica degli errori dell'AI, decisioni documentate), non solo codice. Non contengono segreti.
2. **Riscrivere la storia invece di accettare gli ID nei vecchi commit**: con soli 4 commit il costo era minimo, e gli ID di subscription non servono a nessun lettore.
3. **Claude non fa force-push su `main`**: la riscrittura locale e le verifiche le ha fatte Claude, l'ultimo passo distruttivo lo ha lanciato Luca con il comando fornito.

## Problemi risolti / lezioni

1. **`filter-branch` cancella dal disco i file appena de-tracciati** (li rimuove dal working tree): sono spariti anche i `.pubxml` usati da Visual Studio. Ripristinato dal backup solo il profilo attuale (`Zip Deploy1`, Web App nuova), ora ignorato da Git. Lezione: prima di riscrivere la storia, salvare i file locali che devono sopravvivere.
2. **Caratteri strani incollando nel terminale bash**: era il *bracketed paste* (`^[[200~ … ~`). Soluzioni: pulsante Run nella chat, `bind 'set enable-bracketed-paste off'` per la sessione, oppure `echo 'set enable-bracketed-paste off' >> ~/.inputrc` in modo permanente.

## Problemi ancora aperti (aggiunti/aggiornati)

1. **Il repo è pubblico e la storia originale è stata visibile** fin dal primo push: dopo il force-push GitHub risponde ancora 200 per 2 dei 3 vecchi hash provati (commit orfani raggiungibili solo conoscendo l'hash di 40 caratteri, eliminati col tempo). Rischio giudicato basso (solo ID, nessuna credenziale). Chiusura definitiva, se mai servisse: richiesta a GitHub Support per rimuovere le *cached views*, oppure ricreare il repo (perdendo il collegamento alla SWA).
2. **Nessun segreto da ruotare**: nessuna password è mai finita nel repo.
3. Restano aperti gli altri punti sopra: cancellare le risorse del vecchio account Azure, JWT, CI/CD backend, upgrade .NET, pulizia cartella `TeamCraft.API/` residua e `ai_output_audit.md` con ".NET 7".
4. **Da committare**: questo aggiornamento del log e della memoria non è ancora nel repo (lo fa Luca).
